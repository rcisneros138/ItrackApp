@model IEnumerable<ITrack.Models.Companies>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CompanyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CompanyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CompaniesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CompaniesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CompaniesId })
        </td>
    </tr>
}

</table>
